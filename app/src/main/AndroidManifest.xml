<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alfa11.firebasetemplate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseTemplate">
        <activity
            android:name=".ui.ingredient.IngredientDetailActivity"
            android:exported="true" />
        <activity
            android:name=".ui.recipe.RecipeListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.ingredient.IngredientActivity"
            android:exported="true" />
        <activity
            android:name=".ui.recipe.RecipeActivity"
            android:exported="true" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.registeruser.RegisterUserActivity"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>